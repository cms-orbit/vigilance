<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Sentinel-Hub Base URL
    |--------------------------------------------------------------------------
    |
    | Vigilance가 서버 상태를 전송할 Sentinel-Hub의 도메인 주소입니다.
    | API 엔드포인트 경로(/api/status/report)는 자동으로 추가됩니다.
    |
    */
    'base_url' => env('SENTINEL_HUB_URL', 'https://sentinel-hub.amuz.co.kr'),

    /*
    |--------------------------------------------------------------------------
    | Server UUID
    |--------------------------------------------------------------------------
    |
    | 이 서버를 식별하는 고유 UUID입니다.
    | 설치 시 자동으로 생성되어 .env 파일에 저장됩니다.
    |
    */
    'server_uuid' => env('VIGILANCE_SERVER_ID', ''),

    /*
    |--------------------------------------------------------------------------
    | Group ID
    |--------------------------------------------------------------------------
    |
    | 이 서버가 속할 그룹 ID입니다.
    | 설정 시 서버가 자동으로 해당 그룹에 할당됩니다.
    | 오토스케일링 환경에서 유용합니다.
    |
    */
    'group_id' => env('VIGILANCE_GROUP_ID', null),

    /*
    |--------------------------------------------------------------------------
    | Disk Paths
    |--------------------------------------------------------------------------
    |
    | 모니터링할 디스크 경로 목록입니다.
    |
    */
    'disk_paths' => [
        '/',
        // 추가 경로를 여기에 정의하세요
    ],

    /*
    |--------------------------------------------------------------------------
    | HTTP Health Check URLs
    |--------------------------------------------------------------------------
    |
    | 모니터링할 HTTP 엔드포인트 목록입니다.
    | CMS-Orbit 기반 프로젝트는 기본적으로 /health-check 엔드포인트를 제공합니다.
    |
    */
    'http_health_check_urls' => [
        env('APP_URL', 'http://localhost') . '/health-check',
        // 추가 URL을 여기에 정의하세요
    ],

    /*
    |--------------------------------------------------------------------------
    | Log Monitor Prefixes
    |--------------------------------------------------------------------------
    |
    | 모니터링할 로그 파일 경로 prefix 목록입니다.
    | 데일리 로그(laravel-2025-10-11.log)를 지원하기 위해 prefix 방식을 사용합니다.
    | 예: storage_path('logs/laravel') → laravel.log, laravel-2025-10-11.log 등 모두 매칭
    |
    */
    'log_monitor_prefixes' => [
        storage_path('logs/laravel'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Retry Configuration
    |--------------------------------------------------------------------------
    |
    | API 전송 실패 시 재시도 설정입니다.
    |
    */
    'retry' => [
        'max_attempts' => 3,
        'initial_delay' => 1, // 초
        'max_delay' => 60, // 초
    ],

    /*
    |--------------------------------------------------------------------------
    | HTTP Timeout
    |--------------------------------------------------------------------------
    |
    | API 요청 타임아웃 시간(초)입니다.
    |
    */
    'timeout' => 10,
];

